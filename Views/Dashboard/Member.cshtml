@{
    ViewData["Title"] = "Member Dashboard";
    Layout = "_DashboardLayout";
}

<div class="container-fluid">
    <!-- Quick Access Buttons Row -->
    <div class="row">
        <div class="col-12">
            <div class="card bg-secondary text-white mb-4">
                <div class="card-body">
                    <h2 class="card-title">
                        <i class="fas fa-user me-2"></i>
                        Member Dashboard
                    </h2>
                    <p class="card-text">Welcome, @ViewBag.UserName! Here you can view your activities and society updates.</p>
                </div>
            </div>
        </div>
    </div>
<<<<<<< Updated upstream
@* start *@
=======

    @if (ViewBag.IsEmployeeUser == true)
    {
        <!-- Enhanced Employee Self Attendance Section -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="card border-0 shadow-lg attendance-card">
                    <div class="card-header bg-gradient-info text-white py-4 border-0">
                        <div class="row align-items-center">
                            <div class="col-md-8">
                                <h4 class="mb-2">
                                    <i class="fas fa-user-clock me-3"></i>
                                    Employee Self Attendance System
                                </h4>
                                <p class="mb-0 opacity-75">Mark your daily attendance with just one click</p>
                            </div>
                            <div class="col-md-4 text-end">
                                <div class="employee-badge">
                                    <span class="badge bg-light text-dark fs-6 px-3 py-2">
                                        <i class="fas fa-id-badge me-2"></i>
                                        ID: @ViewBag.EmployeeId
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="card-body p-4">
                        <!-- Status Display -->
                        <div id="emp-attn-status" class="alert alert-info border-0 mb-4 text-center status-display">
                            <i class="fas fa-info-circle me-2"></i>
                            <span id="status-text">Ready to mark your attendance</span>
                        </div>

                        <!-- Animated Check In/Out Buttons -->
                        <div class="row justify-content-center">
                            <div class="col-lg-8">
                                <div class="attendance-buttons-container">
                                    <div class="row g-4">
                                        <div class="col-md-6">
                                            <button id="empCheckIn" class="btn btn-checkin btn-lg w-100 attendance-btn">
                                                <div class="btn-content">
                                                    <div class="icon-container">
                                                        <i class="fas fa-sign-in-alt"></i>
                                                    </div>
                                                    <div class="text-container">
                                                        <span class="btn-title">Check In</span>
                                                        <span class="btn-subtitle">Start your work day</span>
                                                    </div>
                                                </div>
                                                <div class="btn-ripple"></div>
                                            </button>
                                        </div>
                                        <div class="col-md-6">
                                            <button id="empCheckOut" class="btn btn-checkout btn-lg w-100 attendance-btn">
                                                <div class="btn-content">
                                                    <div class="icon-container">
                                                        <i class="fas fa-sign-out-alt"></i>
                                                    </div>
                                                    <div class="text-container">
                                                        <span class="btn-title">Check Out</span>
                                                        <span class="btn-subtitle">End your work day</span>
                                                    </div>
                                                </div>
                                                <div class="btn-ripple"></div>
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Quick Actions Row -->
                        <div class="row mt-4">
                            <div class="col-12">
                                <div class="quick-actions">
                                    <div class="row g-3 justify-content-center">
                                        <div class="col-md-4">
                                            <a href="@Url.Action("MyAttendance", "EmployeeAttendance")" class="btn btn-outline-primary w-100 quick-action-btn">
                                                <i class="fas fa-history me-2"></i>
                                                <span>My Attendance</span>
                                            </a>
                                        </div>
                                        <div class="col-md-4">
                                            <a href="@Url.Action("LeaveRequest", "Dashboard")" class="btn btn-outline-warning w-100 quick-action-btn">
                                                <i class="fas fa-calendar-plus me-2"></i>
                                                <span>Apply Leave</span>
                                            </a>
                                        </div>
                                        <div class="col-md-4">
                                            <a href="@Url.Action("MyLeaves", "EmployeeAttendance")" class="btn btn-outline-info w-100 quick-action-btn">
                                                <i class="fas fa-calendar-check me-2"></i>
                                                <span>Leave Status</span>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Today's Summary -->
                        <div class="row mt-4">
                            <div class="col-12">
                                <div class="today-summary">
                                    <div class="row g-3">
                                        <div class="col-md-3">
                                            <div class="summary-card bg-gradient-success text-white">
                                                <div class="card-body text-center p-3">
                                                    <i class="fas fa-clock fa-2x mb-2"></i>
                                                    <h6 class="mb-1">Today's Status</h6>
                                                    <div id="emp-attn-today" class="fw-bold">Not Started</div>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-md-3">
                                            <div class="summary-card bg-gradient-info text-white">
                                                <div class="card-body text-center p-3">
                                                    <i class="fas fa-calendar-day fa-2x mb-2"></i>
                                                    <h6 class="mb-1">Current Date</h6>
                                                    <div class="fw-bold" id="current-date">@DateTime.Now.ToString("dd MMM yyyy")</div>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-md-3">
                                            <div class="summary-card bg-gradient-warning text-white">
                                                <div class="card-body text-center p-3">
                                                    <i class="fas fa-clock fa-2x mb-2"></i>
                                                    <h6 class="mb-1">Current Time</h6>
                                                    <div class="fw-bold" id="current-time">@DateTime.Now.ToString("HH:mm:ss")</div>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-md-3">
                                            <div class="summary-card bg-gradient-secondary text-white">
                                                <div class="card-body text-center p-3">
                                                    <i class="fas fa-user-tie fa-2x mb-2"></i>
                                                    <h6 class="mb-1">Employee Type</h6>
                                                    <div class="fw-bold">Active</div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <hr class="my-4" />
                        
                        <!-- Tips Section -->
                        <div class="tips-section text-center">
                            <div class="alert alert-light border-0">
                                <i class="fas fa-lightbulb text-warning me-2"></i>
                                <strong>Pro Tips:</strong> 
                                You can change your password from your avatar menu after first login. 
                                Attendance is automatically tracked with timestamps and location data.
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }

    @* start *@
>>>>>>> Stashed changes
    <div class="row">
        <div class="col-xl-4 col-md-6 mb-4">
            <div class="card border-left-secondary shadow h-100 py-2">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">
                                My Activities</div>
                            <div class="h5 mb-0 font-weight-bold">4</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-tasks fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-xl-4 col-md-6 mb-4">
            <div class="card border-left-primary shadow h-100 py-2">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                                Society News</div>
                            <div class="h5 mb-0 font-weight-bold ">3</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-newspaper fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-xl-4 col-md-6 mb-4">
            <div class="card border-left-info shadow h-100 py-2">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-info text-uppercase mb-1">
                                Upcoming Events</div>
                            <div class="h5 mb-0 font-weight-bold ">2</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-calendar-alt fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-6">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-secondary">Member Actions</h6>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <a href="#" class="btn btn-secondary btn-block">
                                <i class="fas fa-eye me-2"></i>View Activities
                            </a>
                        </div>
                        <div class="col-md-6 mb-3">
                            <a href="#" class="btn btn-primary btn-block">
                                <i class="fas fa-newspaper me-2"></i>Read News
                            </a>
                        </div>
                        <div class="col-md-6 mb-3">
                            <a href="#" class="btn btn-info btn-block">
                                <i class="fas fa-calendar-alt me-2"></i>See Events
                            </a>
                        </div>
                        <div class="col-md-6 mb-3">
                            <a href="#" class="btn btn-success btn-block">
                                <i class="fas fa-comment me-2"></i>Give Feedback
                            </a>
                        </div>
                        <!-- New Buttons -->
                        <div class="col-md-6 mb-3">
                            <a href="#" class="btn btn-outline-primary btn-block">
                                <i class="fas fa-bullhorn me-2"></i> Notices
                            </a>
                        </div>
                        <div class="col-md-6 mb-3">
                            <a href="#" class="btn btn-outline-success btn-block">
                                <i class="fas fa-money-bill-wave me-2"></i> Payment & Dues
                            </a>
                        </div>
                        <div class="col-md-6 mb-3">
                            <a href="#" class="btn btn-outline-info btn-block">
                                <i class="fas fa-file-alt me-2"></i> View Documents
                            </a>
                        </div>
                        <div class="col-md-6 mb-3">
                            <a href="#" class="btn btn-outline-danger btn-block">
                                <i class="fas fa-phone-alt me-2"></i> Emergency Contacts
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-secondary">Recent Member Activities</h6>
                </div>
                <div class="card-body">
                    <div class="list-group list-group-flush">
                        <div class="list-group-item d-flex justify-content-between align-items-center">
                            <div>
                                <strong>Attended event</strong>
                                <br><small class="text-muted">Annual General Meeting</small>
                            </div>
                            <span class="badge bg-primary rounded-pill">Today</span>
                        </div>
                        <div class="list-group-item d-flex justify-content-between align-items-center">
                            <div>
                                <strong>Feedback given</strong>
                                <br><small class="text-muted">On cleanliness drive</small>
                            </div>
                            <span class="badge bg-success rounded-pill">Yesterday</span>
                        </div>
                        <div class="list-group-item d-flex justify-content-between align-items-center">
                            <div>
                                <strong>Read news</strong>
                                <br><small class="text-muted">Society newsletter</small>
                            </div>
                            <span class="badge bg-info rounded-pill">2 days ago</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<<<<<<< Updated upstream
=======
@section Scripts {
    <script>
        $(document).ready(function () {
            // Check if jQuery is loaded
            if (typeof $ === 'undefined') {
                console.error('jQuery is not loaded. Please check the jQuery library inclusion.');
                return;
            }

            console.log('jQuery loaded successfully');
            console.log('ViewBag.IsEmployeeUser:', @(ViewBag.IsEmployeeUser == true ? "true" : "false"));
            console.log('Check-in button exists:', $('#empCheckIn').length > 0);
            console.log('Check-out button exists:', $('#empCheckOut').length > 0);
            
            // Show debug information
            console.log('Debug info:', $('#debug-info').html());
            
            // Test basic button functionality regardless of employee status
            $('#empCheckIn').on('click', function(e) {
                console.log('Basic check-in click detected');
            });
            
            $('#empCheckOut').on('click', function(e) {
                console.log('Basic check-out click detected');
            });

            if (@(ViewBag.IsEmployeeUser == true ? "true" : "false")) {
                console.log('Employee user detected, initializing attendance system...');
                
                // Update current time every second
                setInterval(function() {
                    var now = new Date();
                    $('#current-time').text(now.toLocaleTimeString());
                    $('#current-date').text(now.toLocaleDateString('en-US', { 
                        day: '2-digit', 
                        month: 'short', 
                        year: 'numeric' 
                    }));
                }, 1000);

                // Test if buttons exist and add basic click handler
                console.log('Setting up button event handlers...');
                
                // Enhanced Check In functionality
                $('#empCheckIn').on('click', function (e) {
                    e.preventDefault();
                    var btn = $(this);
                    var originalText = btn.find('.btn-title').text();
                    
                    console.log('Check-in button clicked - event handler triggered');
                    
                    // Show loading state
                    btn.prop('disabled', true);
                    btn.find('.btn-title').text('Processing...');
                    btn.find('.icon-container i').removeClass('fa-sign-in-alt').addClass('fa-spinner fa-spin');
                    
                    // Add CSRF token for the request
                    var token = $('input[name="__RequestVerificationToken"]').val();
                    if (!token) {
                        // Try to get token from meta tag
                        token = $('meta[name="__RequestVerificationToken"]').attr('content');
                    }
                    console.log('CSRF Token found:', token ? 'Yes' : 'No');
                    
                    $.ajax({
                        url: '@Url.Action("MarkMemberAttendance", "Dashboard")',
                        type: 'POST',
                        data: {
                            action: 'checkin',
                            __RequestVerificationToken: token
                        },
                        dataType: 'json',
                        success: function (res) {
                            console.log('Check-in response:', res);
                            if (res && res.success) {
                                showStatus('success', res.message || 'Successfully checked in!');
                                if (res.checkInTime) {
                                    $('#emp-attn-today').text('Checked in at ' + res.checkInTime);
                                }
                                // Disable check-in button permanently after successful check-in
                                btn.prop('disabled', true);
                                btn.addClass('btn-success').removeClass('btn-checkin');
                                btn.find('.btn-title').text('Already Checked In');
                                btn.find('.btn-subtitle').text('Ready to check out');
                            } else {
                                showStatus('error', res.message || 'Check-in failed');
                            }
                        },
                        error: function (xhr, status, error) {
                            console.error('Check-in error:', error, xhr.responseText);
                            var errorMessage = 'Check-in failed. Please try again.';
                            if (xhr.responseJSON && xhr.responseJSON.message) {
                                errorMessage = xhr.responseJSON.message;
                            }
                            showStatus('error', errorMessage);
                        },
                        complete: function () {
                            // Restore button state
                            btn.prop('disabled', false);
                            btn.find('.btn-title').text(originalText);
                            btn.find('.icon-container i').removeClass('fa-spinner fa-spin').addClass('fa-sign-in-alt');
                        }
                    });
                });

                // Enhanced Check Out functionality
                $('#empCheckOut').on('click', function (e) {
                    e.preventDefault();
                    var btn = $(this);
                    var originalText = btn.find('.btn-title').text();
                    
                    console.log('Check-out button clicked');
                    
                    // Show loading state
                    btn.prop('disabled', true);
                    btn.find('.btn-title').text('Processing...');
                    btn.find('.icon-container i').removeClass('fa-sign-out-alt').addClass('fa-spinner fa-spin');
                    
                    // Add CSRF token for the request
                    var token = $('input[name="__RequestVerificationToken"]').val();
                    if (!token) {
                        // Try to get token from meta tag
                        token = $('meta[name="__RequestVerificationToken"]').attr('content');
                    }
                    console.log('CSRF Token found:', token ? 'Yes' : 'No');
                    
                    $.ajax({
                        url: '@Url.Action("MarkMemberAttendance", "Dashboard")',
                        type: 'POST',
                        data: {
                            action: 'checkout',
                            __RequestVerificationToken: token
                        },
                        dataType: 'json',
                        success: function (res) {
                            console.log('Check-out response:', res);
                            console.log('Check-out success:', res.success);
                            console.log('Check-out time:', res.checkOutTime);
                            console.log('Total hours:', res.totalHours);
                            if (res && res.success) {
                                showStatus('success', res.message || 'Successfully checked out!');
                                if (res.checkOutTime) {
                                    $('#emp-attn-today').text('Checked out at ' + res.checkOutTime);
                                }
                                if (res.totalHours) {
                                    console.log('Total hours calculated:', res.totalHours);
                                }
                                // Disable check-out button permanently after successful check-out
                                btn.prop('disabled', true);
                                btn.addClass('btn-success').removeClass('btn-checkout');
                                btn.find('.btn-title').text('Already Checked Out');
                                btn.find('.btn-subtitle').text('Completed for today');
                                
                                // Also disable check-in button to prevent multiple check-ins
                                $('#empCheckIn').prop('disabled', true);
                                $('#empCheckIn').addClass('btn-secondary').removeClass('btn-checkin');
                                $('#empCheckIn').find('.btn-title').text('Already Checked In');
                                $('#empCheckIn').find('.btn-subtitle').text('Completed for today');
                            } else {
                                showStatus('error', res.message || 'Check-out failed');
                            }
                        },
                        error: function (xhr, status, error) {
                            console.error('Check-out error:', error, xhr.responseText);
                            var errorMessage = 'Check-out failed. Please try again.';
                            if (xhr.responseJSON && xhr.responseJSON.message) {
                                errorMessage = xhr.responseJSON.message;
                            }
                            showStatus('error', errorMessage);
                        },
                        complete: function () {
                            // Restore button state
                            btn.prop('disabled', false);
                            btn.find('.btn-title').text(originalText);
                            btn.find('.icon-container i').removeClass('fa-spinner fa-spin').addClass('fa-sign-out-alt');
                        }
                    });
                });

                // Enhanced status display function
                function showStatus(type, message) {
                    var statusDiv = $('#emp-attn-status');
                    var statusText = $('#status-text');
                    
                    console.log('Showing status:', type, message);
                    
                    // Remove existing classes
                    statusDiv.removeClass('alert-info alert-success alert-danger alert-warning');
                    
                    // Add appropriate classes and icon
                    if (type === 'success') {
                        statusDiv.addClass('alert-success');
                        statusText.html('<i class="fas fa-check-circle me-2"></i>' + message);
                    } else if (type === 'error') {
                        statusDiv.addClass('alert-danger');
                        statusText.html('<i class="fas fa-exclamation-circle me-2"></i>' + message);
                    } else if (type === 'warning') {
                        statusDiv.addClass('alert-warning');
                        statusText.html('<i class="fas fa-exclamation-triangle me-2"></i>' + message);
                    } else {
                        statusDiv.addClass('alert-info');
                        statusText.html('<i class="fas fa-info-circle me-2"></i>' + message);
                    }
                    
                    // Add animation
                    statusDiv.addClass('status-animate');
                    setTimeout(() => {
                        statusDiv.removeClass('status-animate');
                    }, 500);
                }
                
                // Initialize status
                showStatus('info', 'Ready to mark your attendance');
                
                // Check current attendance status
                checkCurrentAttendanceStatus();
                
                // Function to check current attendance status
                function checkCurrentAttendanceStatus() {
                    $.ajax({
                        url: '/EmployeeAttendance/GetCurrentAttendanceStatus',
                        type: 'GET',
                        dataType: 'json',
                        success: function (res) {
                            console.log('Current attendance status:', res);
                            if (res && res.hasCheckIn) {
                                $('#empCheckIn').prop('disabled', true);
                                $('#empCheckIn').addClass('btn-success').removeClass('btn-checkin');
                                $('#empCheckIn').find('.btn-title').text('Already Checked In');
                                $('#empCheckIn').find('.btn-subtitle').text('Ready to check out');
                                
                                if (res.checkInTime) {
                                    $('#emp-attn-today').text('Checked in at ' + res.checkInTime);
                                }
                                
                                if (res.hasCheckOut) {
                                    $('#empCheckOut').prop('disabled', true);
                                    $('#empCheckOut').addClass('btn-success').removeClass('btn-checkout');
                                    $('#empCheckOut').find('.btn-title').text('Already Checked Out');
                                    $('#empCheckOut').find('.btn-subtitle').text('Completed for today');
                                    
                                    if (res.checkOutTime) {
                                        $('#emp-attn-today').text('Checked out at ' + res.checkOutTime);
                                    }
                                }
                            }
                        },
                        error: function (xhr, status, error) {
                            console.log('Could not check attendance status:', error);
                        }
                    });
                }
            } else {
                console.log('Non-employee user, attendance system not initialized');
            }
        });
    </script>
}

>>>>>>> Stashed changes
<style>
    .border-left-primary {
        border-left: 0.25rem solid #4e73df !important;
    }
    .border-left-success {
        border-left: 0.25rem solid #1cc88a !important;
    }
    .border-left-info {
        border-left: 0.25rem solid #36b9cc !important;
    }
    .border-left-warning {
        border-left: 0.25rem solid #f6c23e !important;
    }
    .border-left-secondary {
        border-left: 0.25rem solid #6c757d !important;
    }
    .text-gray-800 {
        color: #5a5c69 !important;
    }
    .text-gray-300 {
        color: #dddfeb !important;
    }
    .font-weight-bold {
        font-weight: 700 !important;
    }
    .text-xs {
        font-size: 0.7rem;
    }
</style> 
